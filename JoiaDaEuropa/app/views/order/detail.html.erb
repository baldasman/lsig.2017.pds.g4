<!-- g4navbar -->
<nav data-spy="scroll" data-target=".navbar" class="navbar navbar-default navbar-fixed-top">

  <div class="container">
    <div class="navbar-header">
      <a class="button" href="/home/index"><img src="<%= asset_path '2.png' %>" width="190px"></a>
    </div>
    <div class="collapse navbar-collapse">
      <ul class="nav navbar-nav navbar-right">
        <li><a href="/profile/index">PERFIL</a></li>
        <li><a href="/order/show">ENCOMENDAS</a></li>
        <li><a href="/order/new">NOVA ENCOMENDA</a></li>
        <li><input type="text" name="search" placeholder="Search.."></li>
      </ul>
    </div>
  </div>
</nav>

<p/>
<form class="jumbotron">
  <body>
  <header class="row" style="text-align: center;">
    <div class="col-md-3">
      <%= link_to 'PT', '/?lang=pt' %>
      <span>|</span>
      <%= link_to 'EN', '/?lang=en' %>
    </div>
    <div class="col-md-6">

    </div>
    <div class="col-md-3">
      <% if current_user %>
          <%= link_to 'Sign Out', destroy_user_session_path, {method: 'delete'} %>
      <% else %>
          <%= link_to 'Sign In', new_user_session_path %>
      <% end %>
    </div>
  </header>

  <%= yield %>

  <% if notice %>
      <div class="alert alert-info" role="alert">
        <button type="button" class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">&times;</span></button>
        <%= notice %>
      </div>
  <% end %>
  <% if alert %>
      <div class="alert alert-warning" role="alert">
        <button type="button" class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">&times;</span></button>
        <%= alert %>
      </div>
  <% end %>
  </body>
</form>
</div>

    <div class="col-lg-10 text-center">
      <h1><b>DETALHES DE ENCOMENDA</b></h1>
      <i></i>
      <br><br>
      <div class="col-lg-10 text-left">
        <table width="100%" class="table-responsive table-striped">
  <thead>
  <tr>
    <th>Nº</th>
    <th>Descrição</th>
    <th>Data de Entrega</th>
    <th>Preço Unitário</th>
    <th>Quantidade</th>
    <th>Estado</th>
    <th></th>
  </tr>
  </thead>
  <tbody>
      <tr class="odd gradeX">
        <td><%= @order.id %></td>
        <td><%= @order.description %></td>
        <td><%= @order.delivery_date %></td>
        <td><%= @order.unit_price %></td>
        <td><%= @order.quantity %></td>
        <td><%= @order.state_id %></td>
        <td><%= link_to t("edit"), order_edit_path %></td>
      </tr>
  </tbody>
</table>